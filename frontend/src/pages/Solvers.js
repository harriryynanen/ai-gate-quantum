import React from 'react';

const Solvers = () => {
  return (
    <div>
      <h1>Solver Registry</h1>
      <p>This is a placeholder for the solver registry page.</p>
    </div>
  );
};

export default Solvers;
